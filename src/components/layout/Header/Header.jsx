const Header = ({ children }) => {
  return (
    <div>
      <h1

        style={{ marginBottom: "20px", marginTop: "20px", fontSize: "25px", }

        }>{children}</h1>
    </div>
  )
}

export default Header
